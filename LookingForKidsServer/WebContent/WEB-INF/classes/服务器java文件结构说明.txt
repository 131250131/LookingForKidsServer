src内：
1.action包，放Action，实现跳转；
2.bean包，放Javabean，即只有私有属性，属性有getter和setter方法，可以理解为原来的vo，主要用于存储数据；
3.common包，放一些公用的类，暂无；
4.dao包，放的dao类都是interface，相当于原来的data接口层，用来和数据库交互；
  dao.impl包，放的是dao接口的实现；
5.form包，放的是表单数据，形式类似于bean
5.service包，放的是逻辑层的接口；
  service.impl包，放的是逻辑层的实现；

