1.action包是用来写一些跳转；
2.beam包，里面的类都只有属性，getter和setter方法，可以理解为原来的vo，主要用于暂存jsp界面的数据
3.common包打算来放一些公用的东西，目前还是空的
4.dao 里面的dao文件相当于原来的data接口层，是对底层数据的封装，里面都是一些抽象方法；
  dao.impl包中的是dao的一些实现
5.service Impl包里面是逻辑层的一些实现；其他的文件相当于逻辑层的接口

